###-----------------------------------------------------------------###
# Analysis
# Read in RDS files

hallotest <- cbind(conds, store)
hallo2 <- cbind(conds2, data_it_1)#Creates the correct matrix with cond added to the matrix on column 1 to 3

hallo2test <- hallo2[19,]
hallo1test <- hallotest[4,]
# Exclude but mention failed iterations, adapt analysis accordingly

# Average FMI estimate per condition

# Calculate SE and CI of FMI estimates per condition

# Potentially compare FMI estimations with true values

# Make Pretty Graphs